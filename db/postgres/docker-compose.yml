version: '3.8'
services:
  postgres:
    image: postgres:13
    restart: always
    env_file:
      - postgres.env
    ports:
      - '5432:5432'
    volumes: 
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
  db:
    driver: local
