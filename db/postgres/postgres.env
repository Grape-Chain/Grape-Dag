# Default development settings for the local PostgreSQL container.
# Override in a sibling .env (gitignored) for any non-trivial setup.
POSTGRES_USER=grape
POSTGRES_PASSWORD=changeme
POSTGRES_DB=grapedb
